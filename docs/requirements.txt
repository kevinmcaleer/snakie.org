# Build the Snakie documentation:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   make html      # output in _build/html
sphinx>=7,<10
myst-parser>=2
furo>=2024.1
